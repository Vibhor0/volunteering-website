{
  "name": "project-26-01-2025",
  "scripts": {
    "install-client": "cd client && npm install --legacy-peer-deps",
    "build-client": "cd client && npm run build", 
    "build": "npm install --legacy-peer-deps && npm run build-client"
  },
  "dependencies": {
    "mongodb": "^5.8.1",    
    "cors": "^2.8.5",         
    "react-scripts": "^5.0.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.10",
    "mongoose": "^8.9.5"
  }
}
